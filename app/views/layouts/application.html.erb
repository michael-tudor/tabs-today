<!DOCTYPE html>
<html>
  <head>
    <title>tabs.today</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="top-bar">
      <div class="dropdown">
        <%= link_to marks_path, class: 'logo', data: { toggle: 'dropdown' }, 'aria-haspopup': 'true', 'aria-expanded': 'false' do %>
          <i class="icofont-navigation-menu"></i>
          tabs.today
        <% end %>

        <div class="dropdown-menu">
          <% if user_signed_in? %>
            <%= link_to 'Edit profile', edit_user_registration_path, class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= link_to 'Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' }, class: 'dropdown-item' %>
          <% else %>
            <%= link_to 'Sign up', new_user_registration_path, class: 'dropdown-item' %>
            <%= link_to 'Login', new_user_session_path, class: 'dropdown-item' %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="bottom-bar">
      <%= yield %>
    </div>
  </body>
</html>
